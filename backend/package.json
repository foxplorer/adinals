{
  "name": "adinals-overlay-backend",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test \"dist-test/**/*.test.js\"",
    "diff:derivation": "tsc -p tsconfig.test.json && node dist-test/tools/derivationDiff.js"
  },
  "dependencies": {
    "@bsv/overlay": "^2.2.7",
    "@bsv/sdk": "^2.2.15",
    "mongodb": "^6.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.20",
    "typescript": "5.9.3"
  }
}
